<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/side_menu_bg"
    android:orientation="vertical"
    android:paddingLeft="@dimen/xxtralarge_padding"
    android:paddingRight="@dimen/xxtralarge_padding" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/medium_padding"
        android:layout_marginTop="@dimen/medium_padding"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/slide_profileimage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:padding="@dimen/micro_padding"
            android:src="@drawable/ic_launcher" />

        <TextView
            android:id="@+id/slide_profilename"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Juilya Alba" />
    </LinearLayout>

    <ListView
        android:id="@+id/slidelistview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:descendantFocusability="beforeDescendants"
        android:divider="@drawable/white_saprator"
        android:listSelector="@color/set_semi_transparent"
        android:scrollbarSize="@dimen/micro_padding"
        android:scrollbarThumbVertical="@color/blue"
        android:scrollbars="vertical" >
    </ListView>

</LinearLayout>